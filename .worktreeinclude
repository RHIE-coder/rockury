# Claude Code 가 worktree 를 만들 때 함께 복사할 파일 (.gitignore 문법).
#   대상: `claude --worktree`, 서브에이전트 `isolation: worktree`, 데스크톱 병렬 세션.
#   (`scripts/parallel/setup.mjs` 로 만드는 상설 워크트리는 스크립트가 직접 챙긴다.)
#
# 여기 안 적으면 새 worktree 에는 추적되지 않는 파일이 통째로 없다.

# steward 하네스 활성 스위치. 없으면 하네스가 **꺼진 채로** 작업하게 된다 —
# 경로 판정·테스트 의무·게이트가 전부 빠지고, 그 사실이 조용히 지나간다.
.harness-main
