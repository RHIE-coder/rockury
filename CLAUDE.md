# Rockury — Claude Code 진입점

**steward 하네스가 붙어 있다** — 세션 시작 시 규칙이 자동 로드된다. 경로 판정
(consult/direct/hotfix/small/feature/greenfield)·작업 단계·응답 규율·쉬운 말은 steward 가 관장한다.
값·능력 설정은 `.harness/steward/config.yaml`, 활성 하네스는 `.harness-main`.

프로젝트 지침은 **`AGENTS.md` 가 정본**이다 — 아래로 불러온다.
(다른 에이전트 도구는 `AGENTS.md` 를 직접 읽는다.)
**여기엔 아무것도 더 적지 않는다** — 두 곳에 적으면 한쪽만 고쳐져 어긋난다.

@AGENTS.md
