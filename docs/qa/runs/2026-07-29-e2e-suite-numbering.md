# gate run — 2026-07-29 · main · e2e 스위트 번호 구간 규칙 + 13번 충돌 해소

- 기준 커밋(HEAD=부모): b09feb9 (`feat/infra` 를 ff-only 로 받은 직후)
- 범위: 병렬 개발에서 세 서비스가 동시에 같은 스위트 번호를 잡은 것을 정리하고,
  **재발을 기계로 막는** 가드를 넣었다. 어느 서비스에도 안 속하는 공용 결정이라 `main` 에서 했다.

## 무엇이 문제였나 — 3중 충돌이었다
`13-infra-catalog` 와 `13-uiux-spec` 이 겹친 것으로 보고됐지만, 확인해 보니 **`feat/api` 가 이미
`13`–`20` 여덟 개를 잡고 있었다**(미병합). 즉 한 파일만 옮기는 미봉책은 api 가 합류하는 순간 다시 깨진다.

원인은 설계의 대가다: **등록 목록이 없다**(러너가 폴더를 읽는다)는 것이 "공용 파일을 안 건드리고
스위트를 더할 수 있다"는 뜻이면서 동시에 **"번호를 누가 쓰는지 아무도 모른다"** 는 뜻이었다.

## 어떻게 정리했나

**위험한 부분은 건드리지 않았다.** `01`–`12` 는 공용·db·ai 가 섞여 있고 그 내부 순서가
상태 의존(앞 스위트가 만든 설계·연결을 뒤가 쓴다)이라 재배치하지 않았다. 그 뒤를 구간으로 나눴다:

| 구간 | 주인 | 이번 조치 |
|---|---|---|
| `01`–`12` | 기존 블록(공용·db·ai) | 그대로 |
| `13`–`19` | infra | 13–17 사용 중 · **개명 0** |
| `20`–`29` | uiux | `13-uiux-spec` → **`20-uiux-spec`** (개명 1) |
| `30`–`39` | api | 미병합 · rebase 때 13–20 → 30–37 |
| `40`–`49` · `50`–`59` | db · ai 추가분 | 기존 것은 01–12 에 유지 |

**uiux 를 옮긴 이유**는 파일 1개 대 5개, 즉 최소 churn 이다. 마침 `feat/uiux`·`feat/db`·`feat/ai`
세 브랜치가 전부 main 보다 앞선 커밋 **0** 이라(= 그냥 fast-forward 로 받아간다) 개명이 아무도
흔들지 않는 가장 싼 시점이었다.

## 재발 방지 — 사람이 `ls` 로 알아채는 데 기대지 않는다
`e2e/isolation.test.ts` 에 가드 둘을 넣었다(`npm test` 에서 걸린다):
- **번호 중복 금지** — 겹치면 실행 순서가 번호가 아니라 **이름 철자**에 좌우된다.
  실패 메시지에 구간표를 그대로 실어, 다음 사람이 어느 번호를 골라야 하는지 그 자리에서 안다.
- **`meta.name` = 파일 이름** — 개명하면서 안을 안 고치면 체크포인트에 옛 이름이 남는다.

가드가 실제로 잡는지 **일부러 중복 파일을 놓아 실패시켜 확인**한 뒤 지웠다.

## 결과
- typecheck: **PASS**
- test: **PASS** — **1,403 pass** / 4 skip (직전 1,400 → +3 = 새 가드)
- build: **PASS**
- e2e: **PASS** — **18/18 스위트 · 468체크 실패 0 · 미실행 0**
  (⭐ uiux 스위트가 13 → 20 으로 **infra 뒤로 옮겨졌는데도** 전부 통과 — 서비스 간 상태 의존이 없음을 실측)
- surface-verify: **status=ok** · 화면 47개 · 차단 0

## 곁다리 — 병합 뒤 본진에 `npm install` 이 필요했다
`feat/infra` 가 의존성 둘(`@phosphor-icons/react`·`@jmespath-community/jmespath`)을 들여왔다.
lock 파일은 병합으로 따라왔지만 본진 `node_modules` 에는 없어 typecheck 가 먼저 깨졌다 —
코드 문제가 아니다. **의존성을 들여온 브랜치를 병합한 뒤에는 본진에서 `npm install` 한 번.**

## drift
- `AGENTS.md` 불변식 3 · `e2e/README.md` 확장 절에 **구간표와 가드**를 적었다.
- `docs/qa/uiux-ia.md` S10 의 스위트 경로를 새 이름으로 갱신(살아있는 정본).
- 기록물 둘(`2026-07-28-uiux-service.md` · `2026-07-29-infra-rebase-onto-main.md`)은 그때의 사실이라
  고쳐 쓰지 않고 **개명 사실만 덧붙였다** — 이름이 가리키는 곳이 없어지는 것을 방치하지 않는다.
- `13-uiux-spec` 전수 검색: 살아있는 문서에 남은 참조 **0**.

## 남은 것 (api 에이전트 몫)
`feat/api` 가 rebase 할 때 자기 스위트 여덟 개를 `13`–`20` → **`30`–`37`** 로 옮겨야 한다.
안 옮기면 새 가드가 `npm test` 에서 막고 구간표를 띄운다 — 조용히 지나가지 않는다.

## 판정: **PASS**
