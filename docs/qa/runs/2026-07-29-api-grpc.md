# gate run — 2026-07-29 · API gRPC (완전 판정 + 스트리밍 전송)

- 기준 커밋(HEAD=부모): `6fff42f`
- 범위: **gRPC 신설** + 리뷰 5관점 반영(지적 38건 중 high 4 전부 해소).
  1. 순수 로직 — `shared/api/proto.ts`(서술자→응답 모양 · 이름 맞추기 · 서술자 묶기 ·
     모르는 칸 짚기) · `shared/api/grpcTarget.ts`(접속 대상 · 평문+비밀 차단) ·
     `shared/api/reflectionProto.ts`(규약 정의 · 읽기 규칙 · 판정 제외 서비스)
  2. 전송·판정 — `main/api/grpcReflect.ts`(reflection 왕복 · 판 넘기기 · 상한 · 취소) ·
     `main/api/grpcStream.ts`(스트리밍 전송)
  3. 배선 — `main/ipc/api/contract.ts`(gRPC 완전 판정 · 사유 가림) ·
     `main/ipc/api/stream.ts`(붙기 전 차단) · `main/api/streamSession.ts`(통로 한 자리로)
  4. 종류별 능력 — `INTERFACE_META.schemaCoversStreaming` 신설, gRPC 의 `connectUrl` 칸 제거
  5. 화면 — `StreamView.tsx`(전송 이름 어휘 · 붙기 전 차단 · 전송별 placeholder) ·
     `DriftView.tsx`(여러 줄 사유)
  6. 검사 — `e2e/lib/api/grpcServer.mjs`(reflection 켠 **진짜** 서버, 단위·e2e 공용) ·
     `e2e/suites/39-api-grpc.mjs` 신설 · `35-api-stream` 의 낡은 체크 갱신

- 결과:
  - typecheck (`npm run typecheck`): **PASS**
  - test (`npm test`): **PASS** — 2039 pass / 4 skip (이전 1941 → **+98**)
    (신규: `proto.test.ts` 39 · `grpcTarget.test.ts` 16 · `reflectionProto.test.ts` 14 ·
     `grpcReflect.test.ts` 27 / 보강: `drift.test.ts` +2 · `stream.test.ts` +1)
  - build (`npm run build`): **PASS**
  - e2e (`npm run e2e`): **PASS** — ALL PASS, 28/28 스위트 · **776 체크**(이전 750 → +26).
    신규 `39-api-grpc` 23체크. `--no-db` 없이 전량 실행 — 건너뜀 0 · 미실행 0
  - surface-verify (`npm run surface-verify`): **status=ok** · 화면 58개 · 차단 **0** ·
    관찰(baseline 수용) 23

- drift:
  - **정본을 갱신했다**(리뷰가 찾아낸 규칙을 명문화한 것이 대부분이다):
    - `api-contract.md` § drift.complete **AC-6~AC-11 신설** · AC-5 완료 표시 —
      스키마가 덮는 범위 · 메서드 경로로 잇기 · proto3 `optional` 만 nullable ·
      덮는 범위는 종류별 정의에 · **모양을 못 읽은 것을 "없다"고 하지 않기** ·
      원값 그대로 읽기 · 남의 데이터에 상한 · **사유에서도 비밀 지우기**
    - `api-runner.md` § stream.session **AC-7 개정 · AC-7b 신설** — 정의를 못 받으면 안 붙는다 ·
      모양은 서버가 정한다 · 붙었다 판단은 연결 자체 · 취소가 왕복까지 멈춘다 ·
      **암호화 모르면 비밀 안 보낸다** · 선언≠서버면 안 연다 · 붙기 전에 막는다 ·
      **우리 인코딩 실패를 서버 탓으로 안 적는다** · 모르는 칸은 이름을 짚는다 ·
      못 실은 헤더를 조용히 안 버린다
    - `api-service.md` §7 — 열린 항목에서 gRPC 제거(남은 것은 GraphQL subscription · SOAP)
  - 테스트 정의(`docs/qa/`)도 함께 갱신:
    - `api-contract.md` — **005b~005k · 056 신설**, 055 에 스위트 연결
    - `api-runner.md` — **046a~046g · 057 신설**, 042b·055 갱신
  - **커버리지 구멍의 전제가 낡았던 것을 고쳤다**(리뷰 Q-9): 정본이 "이 케이스가 덮는다"고
    적어 둔 것 중 셋이 실제로는 안 덮고 있었다 — `longs`·`defaults`(검사용 서버에 해당 필드가
    없었다) · 바이트 표기(같은 이유) · 판 넘기기(두 케이스 본문이 같았다).
    픽스처에 `big`·`blob`·`never` 를 더하고 서버를 판별로 세워 **실제로 덮게** 만들었다.

- 미검증으로 **남긴 것**(QA 문서에 등재, 조용한 통과 금지):
  - gRPC 자동 재접속이 실제로 도는 것 · `grpcs://`(TLS) 경로 — 검사용 서버가 평문이다
  - 재접속마다 정의를 다시 받아 오는 낭비(동작은 맞다) — 캐시는 새 위험을 만든다
  - 정의 총량 16MB · 서비스 500개 상한에 실제로 닿는 경우
  - `__proto__` 를 서비스 이름으로 쓰는 실제 서버(단위 테스트만 있다)
  - gRPC 화면이 **채워진 상태**의 대비 — `surface-verify` 순회는 빈 상태만 훑는다
