# gate run — 2026-07-29 · Infra 서비스 M1+M2 (카탈로그 · 설계본 · 노드 문서 · 도커 대조)

- 기준 커밋(HEAD=부모): 069875d
- 범위: Infra 서비스를 빈 자리에서 세운 전체 — 카탈로그 형식·탐침 편집기·설계 캔버스·노드 문서(M1),
  로컬 도커 읽기·짝짓기·대조·흡수(M2), 탐침 저장·카탈로그 화면·MCP 읽기 3종(후속).
  파일 71개(신규 58 · 수정 13). 정본 신설 `docs/spec/infra-{catalog,architecture}.md` ·
  `docs/qa/infra-{catalog,architecture}.md`.
- 결과:
  - typecheck (`npm run typecheck`): **PASS**
  - test (`npm test`): **PASS** — 1,060 pass / 4 skip (작업 전 862 → **+198**).
    신규 파일 옆 테스트: schema · extract · status · icon · probeEdit · userCatalog · builtin(카탈로그)
    / nesting · layout · nodeDoc(설계) / match · diff · absorb · liveView(대조) /
    command · store(메인 IPC).
  - build (`npm run build`): **PASS**
  - e2e (`npm run e2e`): **PASS** — 스위트 15/15 · 체크 **289건 실패 0 · 건너뜀 0 · 미실행 0**.
    신규 78체크 — `13-infra-catalog`(35) · `14-infra-design`(26) · `15-infra-reconcile`(17, `needsDb`).
    도커 test-db 컨테이너를 그대로 읽기 대상으로 쓴다.
  - surface-verify: **status=ok** · 화면 38개(leaf 37) · **차단 0건** · 관찰(기준선 수용) 101건.
- 이 회차의 핵심 실측 (말이 아니라 기계로 지켜지는지):
  - **CASE-iarch-087** — 대조·흡수 **전후로 도커 컨테이너 상태가 하나도 안 변하고**, 지워지거나
    새로 생기지도 않았다. "Rockury 는 인프라를 구축하지 않는다"(D1)의 실측 증거다.
  - **CASE-iarch-043** — 흡수 계획 객체에 실물 변경 필드가 **구조적으로 존재할 수 없다**.
  - **CASE-iarch-034** — 안 읽은 종류는 '미구축'이 아니라 **'대조 안 함'**. "0건이었다"와
    "못 읽었다"를 가른다(`infra_snapshot_probes`).
  - **CASE-icat-045** — 우리 이름공간(`cred`·`node`·`arg`)의 자리표시자에 값이 없으면 반드시 던진다.
    남의 `{{…}}`(도커 출력 서식)는 손대지 않고 흘려보낸다(CASE-icat-044).
- drift:
  - **갱신된 정본**: `docs/spec/infra-catalog.md` · `docs/spec/infra-architecture.md`(둘 다 신설) ·
    `docs/qa/infra-catalog.md` · `docs/qa/infra-architecture.md`(둘 다 신설) · `docs/glossary.md` ·
    양쪽 `README.md` 색인.
  - **이번 게이트에서 잡아 고친 거짓 표기**: 명세가 M1·M2 를 `✅ 구현 완료` 라고 적고 있었으나
    실제로는 인수조건 일곱이 미구현이었다. 마일스톤 표를 🟡 로 낮추고
    **「아직 구현 안 된 인수조건」 표를 명세 안에 신설**해 드러냈다. 미구현 항목에는 각 자리에
    `(미구현)` 을 붙였다. — 명세가 코드보다 앞선 것 자체는 잘못이 아니지만, **앞선 줄 모르게
    ✅ 로 적어 두는 것은 거짓말**이다.
    - `design.canvas` AC-6 PNG/SVG 내보내기 (QA `CASE-iarch-074` 는 정의만 있고 도는 코드 없음)
    - `design.canvas` AC-7 노드 검색·포커싱 (있는 검색칸은 종류 팔레트 거르개다)
    - `reconcile.result` AC-3 대조 배지 다이어그램 겹쳐 보기
    - `reconcile.bootstrap` AC-2 흡수 초안 뒤 "무엇부터 채우나" 목록 (행 배지까지만)
    - `providers.credentials` AC-4 연결 시험 버튼
    - `types.presets` AC-3 사용자 프리셋 만들기 (부분 — 탐침 없는 '모양만' 종류를 새로 만드는 길 없음)
    - `types.presets` AC-4 프리셋 승격
  - **의식적으로 미룬 것(위 표에 안 넣음)** — 각자 자기 자리에 사유와 함께 미구현이라 적혀 있다:
    `node-doc.mcp` AC-4(대조 결과 MCP 노출 — 대조 계산이 렌더러에 있어 메인이 못 부른다) ·
    `design.versions` AC-1(설계본 버전 — M1~M3 에서는 만들지 않기로 못박음) ·
    `infra-catalog.actions`(M4) · `infra-architecture.middleware`(M5).
  - **공용 가드 완화 1건과 그 보상**: `migrations.test.ts` · `assemble.test.ts` 가 "분할 전과
    **정확히 같음**"을 요구해 서비스가 자라면 무조건 깨지는 구조였다 → **"잃은 것 없음"(포함)** 으로
    바꾸고, 느슨해진 만큼 `infra_` 접두어 규칙 검사를 새로 넣었다. 명세 영향 없음(공용 규칙 강화).
  - **커버리지 구멍**: QA `CASE-iarch-074`(PNG 내보내기)는 정의가 서 있는데 덮는 코드가 없다.
    지우지 않고 남겨 둔다 — 위 미구현 표와 짝이고, 구현되면 그대로 켜진다.
- 판정: **PASS** (검사 전부 통과 · 명세 거짓 표기는 이 회차에서 시정)
