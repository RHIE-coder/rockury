# gate run — 2026-07-29 · Infra · M4 액션 버튼 · M5 미들웨어(Redis) · 대비 미달 해소

- 기준 커밋(HEAD=부모): ac81ee9
- 범위: **M4**(카탈로그 액션 → 인자 폼 자동 생성 → 확인 → 실행 → 출력 패널, 잠금은 메인이 강제) ·
  **M5 의 Redis**(의존성 0 RESP 클라이언트 · 접속 관리 · 콘솔) ·
  **대비 미달 해소**(`globals.css` 두 줄 + 전 서비스 기준선 재수립).
- 결과:
  - typecheck (`npm run typecheck`): **PASS**
  - test (`npm test`): **PASS** — 1,215 pass / 4 skip (직전 1,150 → **+65**).
    신규: `catalog/actions`(13) · `middleware/resp`(25) · `middleware/redis`(12, 가짜 RESP 서버) ·
    `middleware/console`(11) · 내장 카탈로그 액션 가드 2건.
  - build (`npm run build`): **PASS**
  - e2e (`npm run e2e`): **PASS** — 17/17 스위트 · **351체크 실패 0 · 미실행 0**(직전 315 → +36).
    신규 스위트 `16-infra-actions`(20) · `17-infra-middleware`(16).
  - surface-verify: **status=ok** · 화면 39개(leaf 38) · 차단 0 · **관찰 23**(직전 101 → −78).
- 이 회차의 핵심 실측:
  - **CASE-icat-135** — 화면을 **우회해 창구를 직접 불러도** 읽기 전용 연결의 위험 액션이 거부된다.
    화면에서만 막으면 잠금이 아니라 권유다.
  - **CASE-icat-136** — 위험 액션은 확인 전에는 실물이 그대로이고, 확인하면 **실물이 실제로 바뀐다**
    (`created` 에서 벗어난다). Rockury 가 실물에 닿는 유일한 통로임을 실측한다.
  - **CASE-imw-032** — **실제 Redis 가 PONG 을 돌려준다.** 의존성 0(`node:net` + 직접 구현 RESP)로 붙었다.
  - **CASE-imw-034** — "붙지 못했다"와 "서버가 거절했다"가 화면에서 갈려 보인다.
- 가드가 잡은 것(둘 다 제 일을 했다):
  - `builtin.test.ts` 의 "내장 액션은 전부 danger" 가 깨졌다 — M1 때 액션이 하나뿐이라 **우연히 맞았을 뿐**.
    → **바꾸는 동사면 danger, 읽는 동사면 아님**으로 고치고 **모르는 동사는 통과시키지 않는다.**
  - `CASE-icat-032` 가 아이콘 지도에 없는 `triangle` 을 잡았다(번들에 명시한 것만 들어가는 규칙).
- 대비 미달 결과(키 기준, `check|formFactor|role|text`):
  `infra` 8→0 · `api` 2→0 · `shell` 2→0 · `uiux` 2→0 · `db` 70→15 · `ai` 0→0.
  **새로 생긴 수용 0건** — 회귀 없음. 남은 15건은 DB 서비스의 다른 색 조합이다.
  **곁다리 실측**: 기준선의 `bounds` 는 같은 코드로 두 번 재수립해도 23건 중 8건이 달라진다.
  `findingKey` 가 좌표를 보지 않으므로 판정에는 영향이 없다 — 좌표 diff 로 "남의 서비스를 건드렸다"고
  판단하면 오진이고, 회귀는 **키가 늘었는지**로 봐야 한다.
- drift:
  - `spec/infra-catalog.md` §actions — M4 ✅, AC-2·3 확장, **AC-5~7 신설**(실행 전 미리보기 ·
    `node`/`arg` 이름공간 분리 · 대상 필수), §actions.output **AC-3 신설**.
    AC-4 에 **Vercel·Supabase·Cloudflare 는 모양만 넣었다**는 사실과 이유를 적었다.
  - `spec/infra-architecture.md` §middleware — M5 🟡(Redis 만), **connection·console 섹션 신설**(AC 9개).
    마일스톤 표에 M4 ✅ · M5 🟡 반영.
  - `qa/infra-catalog.md` — S5c(액션 순수) · S5d(내장 액션 가드) · S9(앱 액션) 신설.
  - `qa/infra-architecture.md` — S5c(미들웨어 규약) · S10(앱 미들웨어) 신설.
  - **명세가 코드보다 앞선 자리**: M5 의 RabbitMQ·Kafka·MQTT(§middleware.scope AC-1) —
    🟡 로 표시하고 화면에도 "아직 못 붙음 + 사유"를 띄운다. `node-doc.mcp` AC-4(대조 결과 MCP)는
    여전히 미착수이고 자기 자리에 사유가 적혀 있다.
  - 공용 파일: `globals.css` 두 줄(**사용자 판단으로 이 브랜치에서 처리**) ·
    `e2e/surface/baseline/*.json`(생성물 재수립).
- 판정: **PASS**
