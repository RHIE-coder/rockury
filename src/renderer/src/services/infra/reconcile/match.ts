/**
 * 짝짓기 — **정본은 `@shared/infra/reconcile/match`** 다(메인도 같은 것을 부른다).
 * 여기는 화면 쪽 import 경로를 지키는 통과 지점이다. 근거는 `./types.ts` 주석에.
 */
export { matchResources, type Match, type MatchResult } from '@shared/infra/reconcile/match'
